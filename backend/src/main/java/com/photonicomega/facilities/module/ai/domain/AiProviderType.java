package com.photonicomega.facilities.module.ai.domain;

/** Integration shape of an AI provider. */
public enum AiProviderType {
    LOCAL_OLLAMA,
    OPENAI_COMPATIBLE,
    MANAGED_CLOUD,
    CUSTOM_GATEWAY
}
