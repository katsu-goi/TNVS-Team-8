# Phase 1 — Supabase Edge Function Infrastructure

**Date:** 2026-08-19
**Status:** COMPLETE — spike function deployed and verified against the live project.

## 1. CLI & Project Link (P1.1)

- Supabase CLI `v2.115.0` available via `npx` (no Docker/Deno preinstalled; Deno 2.3.3 installed standalone at `~/.deno/bin/deno.exe` for local type-checking).
- Logged in with owner access token; linked to **`dunijfrvfozwlykpkfhy`** (TNVS team 8, ap-southeast-1).
- `supabase/.temp/` now regenerated to point at the live ref (was stale `nlzfosfyyqileruosebi`).

## 2. Shared Edge Function Libraries (P1.2)

`supabase/functions/_shared/`:

| File | Purpose |
|---|---|
| `config.ts` | Env access (SUPABASE_URL, service-role, JWT, issuer, TTLs). |
| `envelope.ts` | `{success, message, data?, errors?, errorCode?, timestamp}` — mirrors Spring `ApiResponse`. |
| `cors.ts` | CORS headers, preflight handler, JSON response helper. |
| `jwt.ts` | jose HS256 sign/verify, issuer `photonic-omega-facilities`, claims `roles` + `type` (ACCESS/REFRESH) — mirrors `JwtTokenProvider`. |
| `ip.ts` | Secure client-IP from XFF/X-Real-IP only behind private peer — port of `ClientIpResolver`. |
| `db.ts` | Service-role `@supabase/supabase-js` client (RLS bypass; server-side only). |
| `rate-limit.ts` | Tiered window limiter calling `consume_rate_limit_token` RPC. |

Also added `supabase/functions/deno.json` (imports pinning; local type-check only — the platform bundles `npm:` specifiers directly).

## 3. Local + Live Spike Verification (P1.3, P1.6)

Deployed function **`spike`** at `https://dunijfrvfozwlykpkfhy.supabase.co/functions/v1/spike`.

Verified over live HTTPS:
- `GET /spike/health/ping` → `200 {"success":true,"message":"pong",...}`.
- **Client IP:** real source `154.56.114.183` returned even when a forged `X-Forwarded-For: 203.0.113.10` was sent — Supabase edge overwrites the header, so the resolver can't be spoofed.
- `GET /spike/health/imports` → all deps imported and exercised: `@supabase/supabase-js`, `jose`, `bcryptjs`; JWT sign+verify round-trip; service-role DB query returned `rolesCount: 7` (matches live).
- `OPTIONS` preflight → `204` with `Access-Control-Allow-Origin: *`.
- Unknown route → `404` with envelope `{"success":false,"errorCode":"NOT_FOUND",...}`.

Secrets set on project: `JWT_SECRET`, `JWT_ISSUER` (`SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` are platform-injected).

## 4. Rate Limit Table (P1.4)

Applied to live DB (`supabase/migrations/002_add_rate_limit_counts.sql`):
- `public.rate_limit_counts(limit_key, window_start, request_count, ...)` with unique index `(limit_key, window_start)`.
- `public.consume_rate_limit_token(...)` — atomic upsert under `pg_advisory_xact_lock`, returns false past capacity.
- RLS enabled, zero policies (deny-by-default), exec revoked from `public/anon/authenticated`.
- **Verified live:** 4 calls with capacity 3 → `true,true,true,false`; test rows cleaned up (table left empty).

## 5. Stale Ref Cleanup (P1.5)

- `supabase/migrations/00001_create_all_tables.sql:3` header → `dunijfrvfozwlykpkfhy`.
- `docs/claude-enhancement-prompt.md:24` → `dunijfrvfozwlykpkfhy` + notes new migration `002`.
- `supabase/.temp/*` regenerated by link.
- Archival mentions in `docs/analysis/PHASE-0-REPORT.md` / audit are kept intentionally (historical record).

## 6. Files Changed

- New: `supabase/functions/deno.json`, `supabase/functions/_shared/{config,envelope,cors,jwt,ip,db,rate-limit}.ts`, `supabase/functions/spike/index.ts`, `supabase/migrations/002_add_rate_limit_counts.sql`.
- Edited: `supabase/migrations/00001_create_all_tables.sql`, `docs/claude-enhancement-prompt.md`.
- Deployed: function `spike` on `dunijfrvfozwlykpkfhy`; project secrets `JWT_SECRET`, `JWT_ISSUER`.

## 7. Next

Phase 2 (Auth): implement `auth` Edge Function — login (bcrypt verify, lockout rules from `LoginAttemptService`, JWT issuance), refresh (rotation via `refresh_tokens`), logout, password reset flow — reusing the Phase 1 shared libs and verifying against live.
